---
import BitsPageView from '../../components/BitsPageView.astro';
import { getBitsPageData, getBitsPagePath } from '../../lib/bits';
import { PAGE_SIZE_BITS } from '../../../site.config.mjs';

const currentPage = 1;
const { items, yearOptions, tagOptions, totalCount, totalPages } = await getBitsPageData(currentPage, PAGE_SIZE_BITS);
---

<BitsPageView
  items={items}
  resultCount={totalCount}
  currentPage={currentPage}
  totalPages={totalPages}
  yearOptions={yearOptions}
  tagOptions={tagOptions}
  prevPath={null}
  nextPath={totalPages > 1 ? getBitsPagePath(currentPage + 1) : null}
/>
